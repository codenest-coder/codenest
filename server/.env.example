# Create server/.env from this file and add your own secret key. Never commit server/.env.
OPENAI_API_KEY=your_openai_api_key
# Optional model override. The default is gpt-5-mini.
OPENAI_CHAT_MODEL=gpt-5-mini
